# 해당 내용은
의뢰자님에게 설명을 도와드리기 위해 전체적으로 확인 후 구성들을 정리한 내용들이에요.

# 🎉 그룹팅 (Groupting)
**친구들과 함께하는 그룹 소개팅 매칭 앱**
그룹팅은 친구들과 함께 그룹을 만들어 다른 그룹과 매칭되어 소개팅을 즐길 수 있는 Flutter 기반 모바일 애플리케이션입니다.

## ✨ 주요 기능
### 🔐 안전한 회원 관리
- **자물쇠 정보**: 아이디, 전화번호, 생년월일, 성별은 가입 후 변경 불가
- **프로필 관리**: 닉네임, 키, 활동지역, 소개글, 프로필 이미지 설정
- **Firebase Authentication**: 안전한 로그인/회원가입

### 👥 그룹 기능
- **그룹 생성**: 최대 5명까지 그룹 구성
- **그룹 참여**: 초대를 통한 그룹 가입
- **그룹 관리**: 방장 권한으로 그룹 설정 관리

### 💕 스마트 매칭 시스템
- **자동 매칭**: 같은 멤버 수의 그룹끼리 자동 매칭
- **지역 기반**: 활동지역이 겹치는 그룹 우선 매칭
- **매칭 상태**: `대기` → `매칭 중` → `매칭 완료` 단계별 관리

### 💬 실시간 채팅
- **그룹 내 채팅**: 그룹 멤버들과의 실시간 소통
- **매칭 후 채팅**: 매칭된 그룹들 간의 통합 채팅방
- **Firebase Realtime Database**: 실시간 메시지 동기화

### 📨 초대 시스템
- **친구 초대**: 닉네임으로 친구 검색 및 초대
- **초대 관리**: 받은/보낸 초대 목록 관리
- **수락/거절**: 초대에 대한 응답 처리

## 🛠 기술 스택
### Frontend
- **Flutter**: 크로스플랫폼 UI 프레임워크
- **Provider**: 상태 관리
- **Material Design**: UI/UX 디자인

### Backend & Database
- **Firebase Authentication**: 사용자 인증
- **Cloud Firestore**: NoSQL 데이터베이스
- **Firebase Realtime Database**: 실시간 채팅
- **Firebase Storage**: 이미지 저장

### 주요 패키지
```yaml
dependencies:
  flutter: ^3.8.1
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  firebase_database: ^11.2.1
  provider: ^6.1.1
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0
```

## 🏗 프로젝트 구조
```
lib/
├── controllers/          # 상태 관리 컨트롤러
│   ├── auth_controller.dart
│   ├── profile_controller.dart
│   ├── group_controller.dart
│   └── chat_controller.dart
├── models/              # 데이터 모델
│   ├── user_model.dart
│   ├── group_model.dart
│   ├── message_model.dart
│   └── invitation_model.dart
├── services/            # 비즈니스 로직 서비스
│   ├── firebase_service.dart
│   ├── user_service.dart
│   ├── group_service.dart
│   ├── message_service.dart
│   ├── invitation_service.dart
│   └── realtime_chat_service.dart
├── views/              # UI 화면
│   ├── login_view.dart
│   ├── register_view.dart
│   ├── home_view.dart
│   ├── profile_create_view.dart
│   ├── chat_view.dart
│   └── ...
├── widgets/            # 재사용 가능한 위젯
├── utils/              # 유틸리티 및 테마
└── main.dart           # 앱 진입점
```

## 📱 화면 구성

1. **로그인/회원가입**: 이메일 기반 계정 생성
2. **프로필 생성**: 개인 정보 및 프로필 설정
3. **홈 화면**: 그룹 상태, 매칭 현황, 주요 기능 접근
4. **그룹 관리**: 그룹 생성, 멤버 관리, 초대 기능
5. **채팅**: 그룹 내/매칭 후 실시간 채팅
6. **마이페이지**: 프로필 수정, 설정 관리

## 🔄 매칭 시스템 동작 원리

### 1. 그룹 생성 및 대기
- 사용자가 그룹을 생성하고 친구들을 초대
- 그룹 상태: `waiting` (대기 중)

### 2. 매칭 시작
- 그룹 방장이 매칭 시작 버튼 클릭
- 그룹 상태: `matching` (매칭 중)

### 3. 자동 매칭 처리
```
매칭 조건:
✅ 같은 멤버 수 (2명 그룹 ↔ 2명 그룹)
✅ 활동지역 겹침 (강남 ↔ 강남/서초)
✅ 매칭 중 상태의 그룹
```

### 4. 매칭 완료
- 조건에 맞는 그룹과 자동 매칭
- 그룹 상태: `matched` (매칭 완료)
- 통합 채팅방 생성

## 🚀 시작하기

### 1. 환경 설정
```bash
# Flutter SDK 설치 (3.8.1 이상)
flutter --version

# 프로젝트 클론
git clone [repository-url]
cd groupting
```

### 2. 의존성 설치
```bash
flutter pub get
```

### 3. Firebase 설정
1. Firebase 프로젝트 생성
2. Android/iOS 앱 등록
3. `google-services.json` (Android) 및 `GoogleService-Info.plist` (iOS) 추가
4. Firebase 서비스 활성화:
   - Authentication (이메일/비밀번호)
   - Cloud Firestore
   - Realtime Database
   - Storage

#### Firebase 규칙 배포
```bash
# Firebase CLI 로그인
firebase login

# 프로젝트 초기화 (필요시)
firebase init

# 모든 규칙 배포
firebase deploy

# 개별 서비스 배포
firebase deploy --only firestore:rules
firebase deploy --only database:rules
firebase deploy --only storage:rules
```

#### Firebase 에뮬레이터 실행 (개발용)
```bash
# 에뮬레이터 시작
firebase emulators:start

# 특정 서비스만 실행
firebase emulators:start --only firestore,auth
```

### 4. 실행
```bash
# 개발 서버 실행
flutter run

# 빌드
flutter build apk  # Android
flutter build ios  # iOS
```

## 🔒 보안 규칙
프로젝트에는 다음과 같은 Firebase 보안 규칙이 설정되어 있습니다!

### 📄 설정 파일들
- `firestore.rules` - Firestore 데이터베이스 보안 규칙
- `database.rules.json` - Realtime Database 보안 규칙
- `storage.rules` - Firebase Storage 보안 규칙
- `firestore.indexes.json` - Firestore 복합 인덱스 설정

### 주요 보안 정책
```javascript
// 사용자는 본인 데이터만 수정 가능
match /users/{userId} {
  allow read, write: if request.auth.uid == userId;
  allow read: if request.auth != null; // 프로필 조회용
}

// 그룹 멤버만 그룹 데이터 접근 가능
match /groups/{groupId} {
  allow read, update: if request.auth.uid in resource.data.memberIds;
}

// 프로필 이미지는 본인만 업로드/수정 가능
match /profile_images/{userId}/{imageId} {
  allow read, write: if request.auth.uid == userId;
}
```

## 🎯 핵심 개념

### 자물쇠 정보 🔒
가입 시 입력한 다음 정보들은 보안상 수정이 불가능합니다
- 아이디 (로그인 ID)
- 전화번호
- 생년월일  
- 성별

### 그룹 상태 관리
```dart
enum GroupStatus {
  waiting,   // 대기 중 (멤버 모집)
  matching,  // 매칭 중 (다른 그룹 찾는 중)
  matched,   // 매칭 완료 (소개팅 진행)
  inactive   // 비활성 (해체 등)
}
```
## 📄 라이선스
이 프로젝트는 MIT 라이선스 하에 배포됩니다.
자세한 내용은 `LICENSE` 파일을 구성해서 배포하시는 것을 참조해 주시면 좋겠습니다.
---
